apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: technician-server-ingress
spec:
  rules:
    - host: technician.your-domain.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: technician-server-service
                port:
                  number: 8003
