apiVersion: v1
kind: Service
metadata:
  name: customer-server-service
  labels:
    app: customer-server
    component: backend
    service: customer
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
  selector:
    app: customer-server
